funcao vazio principal()
inicio
    inteiro x;
    real y;
    real z;

    x = 10;
    y = 2.5;

    z = x * y;
    imprima(z);

    x = -5 + 10;
    imprima(x);

    x = 10 % 3 + 2 * 4;
    imprima(x);
fim
